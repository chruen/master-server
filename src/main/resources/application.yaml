server:
  address: 0.0.0.0
  port: 8080

# 数据库数据源
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://172.22.185.20:3306/test_schema?useSSL=false@serverTImezone=GMT"
    username: chruen
    password: 12345678
  data:
    redis:
      host: 172.22.185.20
      port: 6379
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
          time-between-eviction-runs: 10s

mybatis-plus:
  type-aliases-package: com.swjtu.robot.masterserver.entity # 别名扫描包

logging:
  file:
    name: log/log.log
  level:
    root: info
    com.swjtu.robot.masterserver: debug

